const tf=async(u,ms=6000)=>{const c=new AbortController(),t=setTimeout(()=>c.abort(),ms);try{const r=await fetch(u,{signal:c.signal,headers:{accept:'application/json','user-agent':'Mozilla/5.0'}});if(!r.ok)throw Error(`${r.status} ${r.statusText}`);return await r.json()}finally{clearTimeout(t)}};const n=v=>{const x=Number(v);return Number.isFinite(x)?x:null};exports.handler=async()=>{try{const j=await tf('https://api-futures.kucoin.com/api/v1/contracts/active');const rows=(j.data||[]).filter(x=>String(x.quoteCurrency||'').toUpperCase()==='USDT').map(x=>{let base=String(x.baseCurrency||'').toUpperCase();if(base==='XBT')base='BTC';return{coin:base,symbol:x.symbol,rate:n(x.fundingFeeRate??x.fundingRate),bid:n(x.bestBidPrice),ask:n(x.bestAskPrice),volume:n(x.volumeOf24h),turnover:n(x.turnoverOf24h),openInterest:n(x.openInterest),mark:n(x.markPrice),index:n(x.indexPrice)}}).filter(x=>x.coin&&Number.isFinite(x.rate));rows.sort((a,b)=>Math.abs(b.rate)-Math.abs(a.rate));return{statusCode:200,headers:{'content-type':'application/json','cache-control':'no-store','access-control-allow-origin':'*'},body:JSON.stringify({exchange:'KuCoin',rows:rows.slice(0,100),timestamp:Date.now()})}}catch(e){return{statusCode:502,headers:{'content-type':'application/json','cache-control':'no-store','access-control-allow-origin':'*'},body:JSON.stringify({error:e.message})}}};